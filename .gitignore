node_modules/
*.db
*.db-*
package-lock.json
.playwright-mcp/

# Local Claude Code settings (may contain credentials)
.claude/

# TTS / ASR models — too large for git, download via scripts/download-models.sh
models/

# Coverage baseline (regenerated locally)
.coverage-baseline.json

# agent-handoff: 本地配置含 chat-id，不入仓
.agent-handoff/config.env

# git worktree 工作树（其他分支的隔离 checkout）
.worktrees/

# 本地 agent 协调脚本（含个人 chat id / 路径）
.agent-handoff/notify-*.sh
